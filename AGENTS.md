# Prompt-Pad

Minimal dark launcher for the Unihertz Titan 2 Elite (4:3, 4.04", top-left camera cutout).
Package `com.hermes.promptpad`. Kotlin + Compose, single activity, zero AI. Weather is the only network feature.

## Build
- `source ~/toolchain/env.sh` first.
- Debug: `./gradlew :app:assembleDebug`; unit tests: `./gradlew :app:testDebugUnitTest`.
- Release: export KEYSTORE_FILE / KEYSTORE_PASSWORD / KEY_ALIAS / KEY_PASSWORD from Bitwarden keys
  `PROMPTPAD_RELEASE_*` (keystore also stored base64 as `PROMPTPAD_RELEASE_KEYSTORE_B64`), then
  `./gradlew :app:assembleRelease`. R8-minified, signed, non-debuggable.
- Test AVD `promptpad43` = 1080x1440 (4:3), android-35 google_apis x86_64, hw keyboard on.

## Layout contract
Two-line lowercase date -> weather/battery caption -> two 57dp bordered glance rows -> four rounded bottom shortcuts. Katapult-derived Lato typography and black/white/orange (`#FC7703`) palette. Date, clock, weather and the four shortcuts are app-picker configurable and gain orange borders in edit mode. Clock defaults to the system alarm view; weather opens the picker until configured. Peak respects safe top insets when the status bar is visible; hidden/right-aligned Peak retains the 8dp corner gap. Home date (all three Peak variants), agenda glance row and to-do glance row are each independently toggleable in Settings > appearance. Notifier, Notes, Agenda and To Do keep content inset-safe with 26sp centered top-edge titles. Notifier and Notes place their filter/folder tabs at the bottom with the list filling from the top; Notes keeps its new-note/reorder row directly above the tabs. An open note uses its editable title as that heading, without a second title row. When "open notifier as home" is enabled, the Notifier is the launcher home and its centered title becomes the current time in the app's orange (headline size retained); no separate top-right time.

## Icon contract
Use the bundled Katapult monochrome icon set for mapped apps and system shortcuts. Note and To Do intentionally retain their PromptPad Material icons. Managed-profile mapped apps use the same bundled artwork with Android's work badge. Drawer icons use one fixed outer size and remain hidden until search text is entered.

## Gestures
Swipe up = profile-aware drawer (personal + managed work apps, badged icons, auto-focused bottom search and Settings button). Swipe right = Notifier when enabled (default on); in the Notifier, swipe right opens Notes; when the Notifier is NOT home, swipe left returns Home; when the Notifier IS home (notifier-as-home), Home is skipped entirely and the Notifier owns Home's gestures — swipe left = your chosen app (`notifierLeftApp`, reconfigurable anytime via Settings > "left-swipe app"; first use with none set opens the picker), swipe up = app drawer (both fire from the empty area even when notifications show, since the list is boxed at the top), double-tap empty area = sleep — while swiping a notification dismisses it. Notifier's Settings toggle also gates drawer and pinned-shortcut entry points. Swipe left on Home = Settings. Long-press blank Home area = shortcut edit mode. Double-tap blank Home area = sleep when enabled. Hidden status bar is transiently revealed by a top-edge swipe. Physical key long-press on Home = mapped app launch. First install shows these instructions once. Back from To Do, including a focused input with the software keyboard, goes to the launcher home (Notifier when notifier-as-home, else Home).

## Removed surfaces
Activity tracking and Focus/Monk restriction modes are intentionally purged, including their permissions, preferences, settings, service, resources, and tests.

## Deliberate simplifications (ponytail)
- Settings only expose behavior wired into the app; delete unused flags and callbacks instead of preserving placeholders.
- Use immutable list updates for row changes; persist mutable note field edits directly.
- Weather uses Open-Meteo only for manual city lookup and MET Norway for forecasts. It fetches only while Home is visible, enabled and cache-expired; no GPS, worker, account or backend.
- Notifier preserves internal `HubListener`, `Screen.Hub` and persisted `promptpad:hub` identifiers. It excludes group summaries and opens activity PendingIntents with Android 14+ sender background-launch opt-in; missing, cancelled and non-activity intents fall back to the app. RemoteInput replies retain original and echoed message text in the live listener state; dismissal removes both and cancels the source notification. Incoming text is read via `NotificationCompat.MessagingStyle` (last 8 messages joined) so WhatsApp/Telegram conversations append like the system tray, falling back to big/plain text otherwise. No notification archive or cross-process reply persistence was added.
- New blank notes focus the title; existing notes open with the body focused and cursor at position zero. The `<>`, `B` and `H` toolbar commands share one cursor-aware insert (`insertAtCursor`): `<>` drops a backtick pair with the cursor between them, `B` drops `****` with the cursor between the two pairs, and `H` inserts a newline+`# ` with the cursor after the hash. No literal `b` placeholder is inserted. View/edit transitions preserve a source-line scroll anchor across markdown rendering; Back closes the note before leaving Notes. Notes and tasks use explicit up/down reorder controls, persisted through the existing store; Notes keeps reorder at the right of New Note, while To Do keeps it at the left of the lowercase `clear` action. Orange scrollbar thumbs appear for overflowing lists and note bodies. No drag-and-drop dependency.
- Notes/To-Do persist as JSON in SharedPreferences; completed To-Dos stay struck through until cleared. Swap for Room only if lists get large.
- Notifier tabs are uniform white Material outline vectors at 22dp — `Icons.Outlined.Call` (Calls), `ChatBubbleOutline` (Messages), `StarOutline` (Starred), `CheckBox` (to-do button opening To Do) — with `All` as text (`All` is default, shown black-on-orange when selected; icons tint black-on-orange when their tab is selected, white otherwise). Material vectors are used over Unicode/FE0E emoji glyphs because `Text(color=)` can't tint an emoji-presentation glyph (📞 stayed dark) and text-dingbat variants depend on device fonts. The email category is purged: no `HubKind.EMAIL`, emails surface under All. Starred items are removed from every other tab and appear only under Starred, whose glyph gains a trailing `°` while it holds anything; starring a notification cancels it from Android's tray but keeps it under Starred (`onNotificationRemoved` retains `starred` items). Tapping a notification's reply focuses the field and raises the keyboard in one step. Swipe right in the Notifier opens Notes. Back from To Do always returns to the launcher home (Notifier when notifier-as-home, else Home).

## Verified release: media dismiss + left-app visible (2026-09-19)
- Branch `fix/media-dismiss-leftapp-visible` from `origin/main`. (1) Media widget was permanently stuck on because the prior "keep watched controller through transient states" never cleared on pause/stop. Now `pick()`/`onPlaybackStateChanged` show it only while `isPlaying` (PLAYING/BUFFERING/CONNECTING); PAUSED/STOPPED clear `state`, releasing the suppression so the app's own dismissible notification returns, and it reappears on the next play. (2) The Settings "left-swipe app" row is no longer gated on `notifierAsHome` — always visible, taps reopen `AppPicker`, shows current label via `shortcutLabel`.
- `:app:testDebugUnitTest`: 25 tests (`isPlaying`). Signed release verified (apksigner v2, non-debuggable, archive clean); release APK installed on `promptpad43` and the "left-swipe app" row confirmed present in Settings via uiautomator. Media play/pause still needs a real device.
- Branch `fix/media-widget-transient` from `origin/main`. Bug: interacting with the media card (skip/pause) made it vanish and Spotify's own notification reappear. Cause: a track skip briefly drops the session into `STATE_BUFFERING`/`STATE_CONNECTING`, so `pick()` found no "active" session, cleared `state`, and released the notification suppression. Fix: `pick()` keeps the already-watched controller while it still exists in the session list, and `isActiveState` now also accepts buffering/connecting. `onSessionDestroyed`/`STATE_STOPPED` still clear it.
- `:app:testDebugUnitTest`: 25 tests (extended `isActiveState` case). Signed release verified (apksigner v2, non-debuggable, archive clean). Media path needs a real device to confirm (no live MediaSession headless).

## Verified release: media widget, left-app, tab icons (2026-09-19)
- Branch `feat/notifier-media-leftapp`, from fetched `origin/main`. Ponytail: native Compose only; media logic adapted from katapult `AudioWidgetHelper` into a trimmed `MediaWidget.kt` (no new dependency); reused the existing `AppPicker`/`launchShortcut` and `IntPref`.
- Changes: (1) `MediaWidget` (MediaSessionManager) pins a play/pause/prev/next card at the top of the Notifier only while a session is playing/paused; the playing app's own notification is suppressed from the list (`add()` skips it; `MediaWidget.update` purges any already-listed). (2) notifier-as-home left swipe now opens `prefs.notifierLeftApp` via shared `launchShortcut` (native screen / system action / app), showing the reused `AppPicker` when unset and persisting the pick. (3) Notifier tabs converted to white Material outline vectors (Call/ChatBubbleOutline/StarOutline/CheckBox) — robust tinting vs. the old FE0E emoji glyphs. (4) instructions rewritten concise and gated on `instructionsSeenVer < INSTRUCTIONS_VERSION` so content changes re-show once.
- `:app:testDebugUnitTest`: 25 tests, zero failures (added `MediaWidget.isActiveState`). Signed release: `apksigner verify` v2 rc=0, manifest non-debuggable, archive free of emulator/androidTest paths.
- API 35 headless `promptpad43`: instructions screen, 📞→Material Call glyph white/aligned, left-swipe picker→launch, and empty-area swipe-up/left with notifications present all verified by screenshot. Media widget couldn't be shown headless (no signed-in media app to create a live MediaSession); covered by unit test + code path. Artifact: `app/build/outputs/apk/release/prompt-pad-release.apk`.

## Verified release: notifier nav & tabs (2026-09-18)
- Branch `feat/notifier-nav-tabs`, created from fetched `origin/main` at `13434d9`. Full ponytail audit: native Compose only, no dependency/schema/abstraction added; reused the existing edit-mode border pattern, `FormatListBulleted` icon and `EdgeScreen(heading=)`/`topRight=` params.
- Changes: (1) right swipe in Notifier opens Notes via `nav(Screen.Notes)`; (2) notifier-as-home title is now the lowercase short date with the time top-right; (3) tapping Reply focuses the input and shows the keyboard in one step (`focus.requestFocus(); keyboard?.show()`); (4) `HUB_FILTERS` reordered to `Calls, Messages, All, Starred`, All still default; (5) starred notifications leave all other tabs and live only under Starred, whose tab shows an orange border when non-empty; (6) email category purged (`HubKind.EMAIL` and its `kindOf` branch removed), Starred moved into email's old slot, and a second checklist button (orange `FormatListBulleted`, 18dp) added opening To Do.
- `:app:testDebugUnitTest`: 24 tests, zero failures (updated HUB_FILTERS assertion). Signed release build succeeds; `apksigner verify` passes (rc=0, v2), `apkanalyzer manifest debuggable` false.
- API 35 headless `promptpad43`: signed release installs and cold-launches with no AndroidRuntime crash; HomeActivity resumes. Artifact: `app/build/outputs/apk/release/prompt-pad-release.apk`.

## Verified release: notifier / notes polish (2026-09-10)
- Branch `feat/notifier-notes-polish`, created from fetched `origin/main` at `e2e8b1b`.
- Principal ponytail audit completed: native APIs/existing Compose only, no added dependencies or schema migration. Removed obsolete note-field styling; fixed notification trampolines, all-day UTC/local-date boundaries and software-keyboard obstruction of task reorder controls during validation. Existing signing identity retained.
- `:app:testDebugUnitTest`: 21 tests, zero failures/errors/skips. Covers reorder bounds, source-line scroll mapping, initial cursor, 90% selection, local midnight/DST and all-day filtering. `:app:lintRelease`: zero errors; existing warnings remain. `git diff --check` clean.
- API 35 headless `promptpad43` instrumentation passes with software IME enabled: actual notification intent and both fallback launches, inline reply retention, row swipe/system removal, note focus/title/scroll/reorder, To Do Back/reorder, Notifier toggle/gestures, 90% preference, status inset comparison and weather app selection/opening. Status-visible and edit-mode screenshots checked for clearance/orange borders.
- Runner: build `:app:assembleDebug :app:assembleDebugAndroidTest`; install both debug APKs; grant the test package `POST_NOTIFICATIONS`; enable `com.hermes.promptpad/.HubListener` using `cmd notification allow_listener`; run `adb shell am instrument -w com.hermes.promptpad.test/com.hermes.promptpad.RegressionRunner`. Require explicit `PASS`, not merely adb exit zero. Fixtures overwrite emulator app notes/tasks: use a disposable AVD, never a user's device.
- Signed/minified release separately installed and smoke-tested through onboarding, empty-note focus, saved-note cursor/typing, To Do Back with IME, and Notifier round trip; no AndroidRuntime crashes. `apksigner verify` passes and `apkanalyzer manifest debuggable` is false. Artifact: `app/build/outputs/apk/release/prompt-pad-release.apk`. WhatsApp itself was not installed; notification behavior was exercised with a real Android notification fixture, not a mocked listener.

## Verified release: notes / to-do controls (2026-09-11)
- Branch `feat/notes-todo-controls`, created from fetched `origin/main` at `bb539c1`. Full ponytail audit retained native Compose only and narrowed the heading inset to the status-bar edge; no dependency, schema or abstraction was added.
- `:app:testDebugUnitTest`: 22 tests, zero failures/errors/skips. `:app:lintDebug`, debug/test APK builds and `git diff --check` pass.
- API 35 `promptpad43` instrumentation passes with software IME: status-visible Notes/To Do headings, both requested reorder-row placements, new-note title focus, backtick insertion, note/task reorder and the existing regression suite.
- Signed/minified release installs and cold-launches without an AndroidRuntime crash. APK Signature Scheme v2 verification passes and the manifest is non-debuggable. Artifact: `app/build/outputs/apk/release/prompt-pad-release.apk`.

## Verified release: ui polish refactor (2026-09-17)
- Branch `feat/ui-polish-refactor`, created from `main`. Full ponytail audit: native Compose only, no dependency/schema/abstraction added. The three toolbar inserts were unified onto one `insertAtCursor` and the now-dead `append` helper deleted (deletion over addition).
- Changes: To Do action relabelled `Clear`→`clear`; Home date shortened `EEEE, MMMM d`→`EEE, MMM d` across all three Peak variants; Notifier notification body capped at `maxLines = 5` with ellipsis; Notes `B`/`H` toolbar commands now match `<>` cursor-aware insertion (`****` cursor-between, `\n# ` cursor-after-hash, no literal `b`).
- `:app:testDebugUnitTest`: 24 tests, zero failures (added bold/heading cursor cases). `:app:lintRelease` zero errors. Signed release build succeeds; `apksigner verify` passes (v2), manifest non-debuggable. Artifact: `app/build/outputs/apk/release/prompt-pad-release.apk`.

## Verified release: home & notifier toggles (2026-09-18)
- Branch `feat/home-notifier-toggles`, created from `main` at `91e07b8`. Full ponytail audit: native Compose only, no dependency/schema/abstraction added (`NotificationCompat.MessagingStyle` from the already-present androidx.core; one added optional `EdgeScreen(topRight=)` param).
- Changes: Settings > appearance gains independent "date in peak widget", "agenda on home", "to-do on home" and "open notifier as home" toggles (agenda/to-do split, not one combined flag). Notifier and Notes filter/folder tabs moved to the bottom with lists filling from the top; Notes new-note/reorder row sits directly above its tabs. "open notifier as home" makes Notifier the launcher home (routing via `homeScreen`, back/onNewIntent honour it) showing only the time top-right, cutout-inset. Notifier incoming text now reads MessagingStyle so same-chat WhatsApp/Telegram messages append instead of replacing the latest.
- `:app:testDebugUnitTest`: 24 tests, zero failures. Debug build clean. Signed release build succeeds; `apksigner verify` passes (rc=0), manifest non-debuggable.
- API 35 headless `promptpad43`: signed release installs and cold-launches with no AndroidRuntime crash; onboarding→Home→Settings shows all new toggles; enabling "open notifier as home" then Back lands on Notifier-as-home with only the time top-right and tabs at the bottom. Artifact: `app/build/outputs/apk/release/prompt-pad-release.apk`. WhatsApp not installed; MessagingStyle append verified by code path, not a live WhatsApp thread.
